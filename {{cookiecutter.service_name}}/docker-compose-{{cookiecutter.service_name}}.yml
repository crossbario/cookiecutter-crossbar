image: {{cookiecutter.image}}
network_mode: host
user: "1000"
ports:
  - {{cookiecutter.listening_port}}
volumes:
  - ${PWD}/{{cookiecutter.service_name}}:/node
entrypoint: /usr/local/bin/crossbar
command:
  - start
  - --personality
  - community
  - --cbdir
  - /node/.crossbar/
